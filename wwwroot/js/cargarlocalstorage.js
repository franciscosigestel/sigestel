//// Recuperar valores del localStorage y aplicar al formulario al cargar la página
////$(document).ready(function () {
////    var numpag = localStorage.getItem('numpag');
////    var cantidadRegistros = localStorage.getItem('cantidadRegistros');

////    // Aplicar los valores al formulario
////    if (numpag && cantidadRegistros) {
////        actualizarFormulario(numpag, cantidadRegistros);
////    }
////});